# AWS Dashboard - EC2 Integration Environment Configuration
# Copy this file to .env and update with your actual values

# AWS Configuration
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# DynamoDB Configuration (existing EC2 infrastructure)
# Set DDB_ENDPOINT_URL to None or comment out to use production EC2 DynamoDB
# DDB_ENDPOINT_URL=http://localhost:8000  # Only for local development
EXISTING_DDB_TABLE_NAME=UGVTelemetry
EXISTING_LIDAR_TABLE_NAME=UGVLidarScans
ULTRASONIC_TABLE_NAME=UGVUltrasonic
BATTERY_TABLE_NAME=UGVBattery

# Device Configuration
DEVICE_ID=ugv-1

# GPS Configuration (simpleRTK2B - already connected to ArduPilot)
GPS_SERIAL_PORT=COM3
GPS_BAUD=115200

# LiDAR Configuration (RPLIDAR - simplified for object avoidance)
RPLIDAR_PORT=COM4
RPLIDAR_BAUD=256000

# Ultrasonic Configuration (Maxbotix I2C EZ4 - ArduPilot RNGFND_TYPE=2)
I2C_BUS=1
MAXBOTIX_I2C_ADDRESS=0x70
RNGFND_MAX=700

# Battery Monitoring (Pixhawk - 11V RTL threshold)
PIXHAWK_PORT=/dev/ttyACM0
PIXHAWK_BAUD=115200
RTL_VOLTAGE_THRESHOLD=11.0

# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# EC2 Connection Notes:
# 1. Login to AWS with Artem's credentials
# 2. Navigate to EC2 > Instances
# 3. Select running instance and click "Connect"
# 4. Choose "RDP Client" tab
# 5. Select "Connect using Fleet Manager"
# 6. Use provided .pem key file
# 7. May require instance restart if connection fails
